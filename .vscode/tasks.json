{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Release",
            "type": "shell",
            "command": "cmake --build --preset windows-vs2026-release",
            "group": { "kind": "build", "isDefault": true },
            "dependsOn": "Configure"
        },
        {
            "label": "Build Debug",
            "type": "shell",
            "command": "cmake --build --preset windows-vs2026-debug",
            "group": { "kind": "build" },
            "dependsOn": "Configure"
        },
        {
            "label": "Configure",
            "type": "shell",
            "command": "cmake --preset windows-vs2026"
        }
    ]
}